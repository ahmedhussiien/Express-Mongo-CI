{
  "name": "express-mongo-ci",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "jest --verbose",
    "docker:build_test": "docker-compose -f docker-compose.test.yml build",
    "docker:post_run_test": "docker-compose -f docker-compose.test.yml down",
    "docker:run_test": "docker-compose -f docker-compose.test.yml up --exit-code-from api --abort-on-container-exit && npm run docker:post_run_test",
    "docker:test": "npm run docker:build_test && npm run docker:run_test"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.12.7"
  },
  "devDependencies": {
    "jest": "^26.6.3",
    "supertest": "^6.1.3"
  }
}
